SDK Installation:
	Install Python 3.9 (include pip and path tickboxes during install).
	Open a Command Prompt and enter the AccessData SDK Directory.
	Run the setup.py with the "install" parameter as shown below:
		setup.py install

Usage:
	Within a Python script, simply use:
		import accessdata

	Then to create a client use:
		client = accessdata.client.Client("http://localhost:4443/", "<apikey>")